/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TagPipe } from './tag.pipe';

describe('Pipe: Tage', () => {
  it('create an instance', () => {
    let pipe = new TagPipe();
    expect(pipe).toBeTruthy();
  });
});
